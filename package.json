{
  "name": "streamer-board-vote-room",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "test": "node --test tests/*.test.js",
    "test:harness": "node harness/load-scenario.js --url ws://localhost:8787/ws --viewers 200",
    "wrangler:dev": "wrangler dev worker/index.js --local",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "vite": "^6.3.5",
    "typescript": "^5.8.3",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "wrangler": "^4.18.0"
  },
  "devDependencies": {}
}
